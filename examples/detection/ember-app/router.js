// Detection fixture only. A real Ember app keeps this at "app/router.js",
// but that directory name collides with a11yst's Next.js "app router"
// heuristic, so this fixture keeps it flat at the project root instead.
export default {};
