# vue-vite

Vue 3 + Vite SPA with explicit vue-router routes mounted on `#app`.

## Expected routes

| Path | Finding |
| --- | --- |
| `/` | None |
| `/issues` | `button-name` (empty `<button>`) |

## Commands

```bash
pnpm dev
PORT=5600 pnpm dev
```
